[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "avis"
version = "0.1.0"
description = "AVIS database layer"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "SQLAlchemy>=2.0,<3.0",
  "alembic>=1.13,<2.0",
  "psycopg[binary]>=3.2,<4.0",
  "fastapi>=0.115,<1.0",
  "uvicorn>=0.30,<1.0",
  "PyJWT>=2.9,<3.0",
  "cryptography>=43,<46",
  "requests>=2.32,<3.0",
  "pandas>=2.2,<3.0",
  "yfinance>=0.2,<1.0",
  "httpx>=0.27,<1.0",
  "aiosqlite>=0.20,<1.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8,<9",
  "ruff>=0.6,<1.0",
]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
select = ["E", "F", "I"]

[tool.ruff.per-file-ignores]
"tests/*" = ["E501"]

[tool.setuptools.packages.find]
include = ["avis*", "etl*", "apps*"]
